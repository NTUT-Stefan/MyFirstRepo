from Trip.Trip import Diary
import json
# unfinished

def DiaryOutputToJson(diarys):
    # datas = []
    # for d in diarys:
    #     datas.append({
    #     'datetime': d.getDateInfo(),
    #     'ID': d.getIdInfo(),
    #     'category': d.getCategoryInfo(),
    #     'keep': d.getKeepInfo(),
    #     'problems': d.getProblemsInfo(),
    #     'trying': d.getTryingInfo()
    #     })
    # with open('log.txt', 'w') as json_file:
    #     output = json.dumps(datas, indent=4)
    #     json_file.write(output)

# def JsonInputToDiarys():
#     with open('log.txt') as json_file:
#         data = json.load(json_file)
#     data_dict_list = []
#     for d in data:
#         {'category':category, 'keep':keep, 'problem':problem, 'trying':trying}
#         print('datetime: ' + d['datetime'])
#         print('ID: ' + d['ID'])
#         print('category: ' + d['category'])
#         print('keep: ' + d['keep'])
#         print('problems: ' + d['problems'])
#         print('trying: ' + d['trying'])